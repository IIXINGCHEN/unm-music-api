# unm-music-api

## Agent skills

### Issue tracker

Issues 记录在 `IIXINGCHEN/unm-music-api` 的 GitHub Issues 中，通过 `gh` CLI 操作。见 `docs/agents/issue-tracker.md`。

### Triage labels

五个标准分流角色使用默认标签字符串。见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局：仓库根目录的 `CONTEXT.md` + `docs/adr/`。见 `docs/agents/domain.md`。
